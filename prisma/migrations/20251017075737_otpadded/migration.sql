BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Collection] ADD [otp] NVARCHAR(1000) CONSTRAINT [Collection_otp_df] DEFAULT '';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
