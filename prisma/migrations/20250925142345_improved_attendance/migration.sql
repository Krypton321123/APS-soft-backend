BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Attendance] ADD [attendancePhoto] NVARCHAR(1000) CONSTRAINT [Attendance_attendancePhoto_df] DEFAULT '';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
