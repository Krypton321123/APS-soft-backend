BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[LocationNames] ADD [gwncd] NVARCHAR(1000) NOT NULL CONSTRAINT [LocationNames_gwncd_df] DEFAULT '';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
