BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Collection] ADD [imageUrl] NVARCHAR(1000) CONSTRAINT [Collection_imageUrl_df] DEFAULT '';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
