BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Collection] ADD [verified] BIT NOT NULL CONSTRAINT [Collection_verified_df] DEFAULT 0,
[verifiedAt] DATETIME2;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
