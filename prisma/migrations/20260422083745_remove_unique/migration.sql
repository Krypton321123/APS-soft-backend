BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[AcceptedOrders] DROP CONSTRAINT [AcceptedOrders_order_id_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
