BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Collection] ADD CONSTRAINT [Collection_upiId_df] DEFAULT '' FOR [upiId];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
