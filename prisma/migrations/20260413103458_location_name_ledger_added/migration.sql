BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[LocationNames] ADD [cashLedgerId] NVARCHAR(1000) NOT NULL CONSTRAINT [LocationNames_cashLedgerId_df] DEFAULT '';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
