BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[LocationNames] ADD [untcd] NVARCHAR(1000) NOT NULL CONSTRAINT [LocationNames_untcd_df] DEFAULT '';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
